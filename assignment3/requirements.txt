# Assignment 3 (Python 3.10+) runtime dependencies
#
# The original CS231n requirements were pinned for much older Python/Jupyter.
# After upgrading the environment (e.g. to run modern PyTorch / new GPUs),
# those pins will fail to install (e.g. pyzmq==18.0.1, numpy==1.19.5).

numpy>=1.23,<3
scipy>=1.10,<2

matplotlib>=3.7,<4
pillow>=9
imageio>=2.31
tqdm>=4.66

requests>=2.31

ipython>=8
ipykernel>=6
jupyter>=1
notebook>=7
jupyterlab>=4
ipywidgets>=8

# Keep pyzmq modern to avoid invalid legacy metadata on new pip/Python.
pyzmq>=25